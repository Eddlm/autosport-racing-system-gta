# ARS — Deferred technical notes (companion to AGENTS.md)

> Split from `AGENTS.md` (`8447d75`) to keep AGENTS.md under the DSH auto-load size cap
> (~65 KB — AGENTS.md had grown to ~73 KB and its tail was silently truncated).
> AGENTS.md keeps one-line pointers to the sections below. The moved blocks below are
> verbatim, unedited. Reference material and long-form details that are not load-bearing
> for everyday work; append further deferred notes here.

**Read this when**: the work touches **natives, hashes, memory offsets/pointers, marker or rendering detail, ini key lists, settings-repair internals, UI/item inventories, a formula or its derivation, or "why was X removed?"** — and it holds everything the AGENTS.md size trims moved out.

## Nitrous system facts (researched, The Contract-era natives)

- **Nitrous system facts (researched, The Contract-era natives)**: "vehicle has nitrous" = **vehicle mod slot 17** (`VMT_NITROUS`) — detect with `GET_VEHICLE_MOD(veh, 17) != -1` (already in SHVDN2's Hash enum; what LS-Tuners-style shops and trainers write). All nitrous natives are plain game-binary VEHICLE natives (SP-safe, raw-hash calls): `SET_OVERRIDE_NITROUS_LEVEL` 0xC8E9B6B71B8E660D (**must pass all 6 args** — 2-arg calls zero out level/power/recharge on b2060+), `FULLY_CHARGE_NITROUS` 0x1A2BCC8C636F9226, `CLEAR_NITROUS` 0xC889AE921400E1ED, `IS_NITROUS_ACTIVE` 0x491E822B2C464FE4 (BOOL, boost firing now), `_GET_REMAINING_NITROUS_DURATION` 0xBEC4B8653462450E (float charge), `SET_NITROUS_IS_ACTIVE` 0x9E566EA551F4F1A6, `_SET_NITROUS_IS_VISIBLE` 0x465EEA70AF251045, `_SET_REMAINING_NITROUS_DURATION` 0x1AD0F63A94E10EFF, `_SET_VEHICLE_USE_HORN_BUTTON_FOR_NITROUS` 0x1980F68872CC2C3D (global horn→nitrous). Installed state persists in mod data; charge is runtime-only per entity. Vanilla SP LS Customs does NOT sell nitrous (Online workshop feature) — in SP, slot 17 is set by trainers/modded shops only. **Verified in-game**: `_GET_REMAINING_NITROUS_DURATION` is useless for detection (always reads ~full, only drops on fire, never recharges exactly — a below-full charge implies a past fire but is unreliable and resets per entity), and trainer-forced nitro has no detectable installed state — the only fire-side signal is `IS_NITROUS_ACTIVE`. **ARS detection is therefore a latch**: `PlayerHasNitro` turns true on mod slot 17 installed, the player firing nitro (`IS_NITROUS_ACTIVE`, probed in OnTick), OR a below-full charge read; it is cleared at each race start (`StartRace` — a per-race latch, not a whole-session one). A silent OnTick probe (no debug display) runs the poll. **Menyoo 2.0 source audit** (cloned `itsjustcurtis/MenyooSP` to `_research\Menyoo2`): Menyoo declares the full nitrous native set but **never calls any of it** — its "Race Boost On Horn" is fake nitro (`SET_VEHICLE_BOOST_ACTIVE` toggled one frame + a raw forward force + hand-rolled NOS PTFX); its "Infinite Native Boost" targets the separate **rocket-boost family** (Oppressor-style, unrelated to nitrous). This explains why trainer "nitro" is undetectable: it never engages the real nitrous system, so neither charge nor IS_NITROUS_ACTIVE can see it; only slot 17 or genuinely-fired nitrous can latch. Menyoo also has **no vanilla nitrous toggle at all** (its `VehicleMod` enum names slot 17 `Unknown17`). Also note an ecosystem hash-name conflict: Menyoo binds 0x465EEA70AF251045 as `SET_NITROUS_IS_ACTIVE` (our notes call it `_SET_NITROUS_IS_VISIBLE`), so trust behavior over name for that hash.

## Two-projection route speed (future idea)

- **Two-projection route speed (future idea)**: the user rates the projection layer very highly — long-term, `followTrackSpd`'s geometric `√(g·grip·r)` could be replaced by a two-projection system: a *true* projection (ballistic `ProjectAhead`, self-calibrating via measured acceleration — no grip-model disagreement) and a *pessimistic* projection at reduced grip ("could I still hold this if the surface worsens?"). Design caveats the rebuild must solve: a projection is a **limiter, not a planner** — it assumes current velocity+accel continue, so it cannot see future braking decisions (the apex braking map stays for long-horizon work; projections cap what the plan may attempt); multi-second ballistic horizons are physically wrong; and a target derived from a projection that depends on current speed risks a self-referential feedback loop (needs damping or latched verdicts). The pessimistic variant's real value is anticipatory: ease speed *before* the edge instead of the current offshoot cap's after-the-fact emergency. Reference pattern: `ApplyOffshootBlend`'s shape.

## TRlat ladder

- **TRlat ladder — the slide-response envelope is parameterized by `Handling.LateralTractionCurve` alone.** Every slide-handling threshold is a fraction of it, handing off in sequence as a slide grows: **×0.2** = the steer-limiter allowance base; **×0.3** = the countersteer *priority* blend start; **×0.5** = that allowance's ceiling (`maxSteerAngle = min(TRlat × 0.2 + |slide|, TRlat × 0.5)`, so the base is always 40% of the ceiling); **×0.6** = priority saturation and the full-countersteer pedal gate. The **×0.2** rung is live again as the allowance base; do not confuse it with the removed MaxBrake rampdown's threshold, which was `2 + TRlat × 0.2` — a different use of the same fraction. **Re-verified against the source and corrected** — this note used to claim ×0.5 for the blend start and a "×1.2 slideScale ceiling" that exists nowhere in the code. Two things to keep in mind: the blend lerps the final steer toward "trajectory steering − countersteer correction" (the heading/recovery/side-by-side PD plus yaw damping with the countersteer correction applied, **lane pursuit dropped**), with the correction itself at **2 × slip**; and the old `+2°` base was replaced by that TRlat-scaled one because the base *is* a non-sliding car's whole allowance: a high-grip car slides least, so it lives in the base regime and could barely steer (found in game), while the `×0.5` ceiling it was sized for stayed unreachable — the same fixed 2° felt right only on low-TRlat cars. The flip side: a scaled base is **tighter** than the flat 2° below TRlat ≈ 10 (1.2° at TRlat 6), so on a very low-grip car the constant — not the ceiling — is the knob if it turns out not to steer enough; ×0.3 would all but reproduce the old flat margin at the low end while still roughly tripling the high-grip allowance. `_slideCountersteerDegrees`, which this note used to cite as the stored countersteer, **no longer exists** — the live value is the local `countersteerTarget`. **Measured band (parsed from the live `Log.log` `TRlat for <car>` lines)**: the field runs ~10.75–12° with the drift variants far above (`ISSI8` 21.75°, `DRIFTCHAV6` 28°), so "a slide" is a per-car quantity up to 2.5× apart within a single race. The `[1,100]` guard's fallback is 22 — check that log line after any handling swap, because every threshold in the ladder doubles if the read falls out of band.

Related durable lesson (kept in AGENTS.md too): **corrections are corrections — never reuse one as an absolute target.** A value stored as "subtract from the PD" (the old per-frame countersteer) carries the *opposite* sign of the steer direction it wants; lerping the final steer toward it as a target steers **into** the slide. This exact mistake inverted the countersteer at full slide priority and was caught in-game (car sliding rightwards steered leftwards, off-track). When blending toward a slide-response target, reconstruct the full target (trajectory terms + correction applied), not the correction alone.

## Fragments condensed out of AGENTS.md (`7575318` restructure, verbatim)

Small details removed from AGENTS.md pointers during the final trim, preserved here:

- **Gs-aware preview / lane-bias scale**: the blend is a hardcoded local `const float blend = 0.5f` in `ComputeSteering`, over the **0.5 s** projection (`LookAhead.HalfSec` + `ProjectAhead(0.5f)`) — there is no menu item and no `ARS` static (the old "Preview Blend" 0.1–1.0 list item, `ARS.GsAwarePreviewBlend` and `_laneGainDivisor` all went with the steering cleanup, `f5de166`); `ProjectAhead`'s acceleration term is **measured, not commanded**.
- **Nitro pacing**: the charge target sits slightly above full so the below-full detection latch (`charge < 2.99`) stays meaningful — **re-check that interplay if the target changes**.
- **Nitro card vetoes (full rationale)**: braking, off-track, big steer angle, low gear on high-geared RWD cars (wheelspin — **AWD exempt**, via handling `fDriveBiasFront` at `0x48` between 0 and 1; offset anchored by the same struct math verified at steering lock `0x80` / traction-lateral `0x98`), the **fairness gate** (`ARS.PlayerParticipating` — AI nitro only arms when the player has nitro, unless the player isn't on the grid), and the **circuit-wrap veto** (the corner gate never fires inside the armed corner's span — the lap-wrap made a just-behind entrance read a full lap away).
- **Dual pace mode**: the Widen Bracket loop widens in **+0.5 steps** (never doubled; **capped**).
- **Always-include pin (removed)**: the `PinModel` test hook force-injected `"tiberius"` / hash `2100457220`.
- **Model stat natives**: the int model hashes live in the code ("hashes in code"); selection debug log strings were `"Pace-matched candidates: N"` / `"BuildPaceIndex: N models indexed"`.
- **ShowEdgeChevrons**: chevrons sit at `TrackHalfWidth` with a **+0.2 m lift**. **ShowAggro** was retired with the dead debug toggles (`f1acebd`) — no code remains, and `DrawRacerDebug` no longer exists either.
- **Rear-end prevention** (from the old AGENTS.md bullet): the Yield maneuver "overrides the speed blend locally"; the 6 m radius, `r.CombinedSize.X` lateral window, 1 m saturation and `rivalSpeed` floor remain as stated in AGENTS.md.
- **Downforce**: the old pointer read "full derivation **+ source paths** in AGENTS-TECHNOTES.md" — the derivation narrative (`m_fDownforceModifier` at `0x0014`, panthere raw 258 was `m_fPopUpLightRotation` at `0x0018`, real value 120, divider `1 + 0.035 × Downforce` matching `CommandGetVehicleMaxTraction`) is captured in AGENTS.md's gotcha; no separate long-form source-path list exists in this file (flagged in the restructure report). **Corrected 2026**: an earlier revision had `0x0010` = `m_fDownforceModifier` and `0x0014` = `m_fPopUpLightRotation`; the real order is `0x10` `m_fInitialDragCoeff`, `0x14` `m_fDownforceModifier`, `0x18` `m_fPopUpLightRotation`, and the code always read `0x0014`.

## Steering limit (`ApplySteerLimits`)

- **Steering limit** (`ApplySteerLimits`): **currently a slide-angle limit** — max steer = `Remap(speed_mph, rampSpeed→0, limitAngle → steer lock)`: full lock at standstill, collapsing to the limit angle by the ramp speed (both in code), applied **only when steering in** — countersteer is exempt, restored per `07d5e57` (the limit exists to stop the car being steered *into* a slide, not to cap the correction that recovers it; the test is the same steer-vs-yaw sign comparison the slew rate uses, and it is what the zombie block's predecessor did). The limit angle is `min(TRlat × 0.2 + |slide angle|, TRlat × 0.5)` — **corrected**: this note used to describe it as "a small margin plus `max(|slide|, grip-based allowance)`", i.e. a max where the code takes a min. Of the things once tied to the limiter, only the **countersteer brake release** remains (the **MaxThrottle cut** was removed as a no-op that would have fought TCS — see AGENTS-BACKLOG — and the MaxBrake *rampdown* (the brake cap easing to 0.8 as the slide grew) was **removed for the WIP release** and returns after the v0.9 feedback (AGENTS-BACKLOG). The release past the full-countersteer threshold still drops `MaxBrake` to zero, which lands as the post-slew `Min`, so the brake is released instantly rather than ramped — but the **cap is now stateful**: it recovers at the `MaxThrottle` rate (`Control.MaxBrake += 2 × TickScale` in `ConvertSpeedToPedals`, mirroring the throttle line beside it), so full braking returns over ~0.5 s instead of on the next tick, and the value self-heals without a per-tick reset. The old **speed-based reduction** (`steer /= 1 + 0.5 * fwdSpeed`, always-on, plus a TRLateral `×0.3` min-steer floor) is **zombie code** — preserved in a `/* ZOMBIE */` comment block inside `ApplySteerLimits` if this slide-angle approach is abandoned. The earlier **empirical peak-memory** system — it learns the steer angle that produced the best *measured* lateral G. **Its remains are deleted** — `GripRefGain`, `ExploitUtilization`, `UtilizationFloorFactor`, `SpikeCapFactor`, `OversteerCutMargin`, `_latGRef`, `_peakSteerDeg` and `_nearGripPeak` had no readers left (the constants were the only survivors of that system), so the whole paragraph below is the record of *removed* code, not live behavior; recover the values from git if it is ever rebuilt. The `MaxThrottle` cut is additionally gated on the limiter having authority (see the steer-limiter tie-in in AGENTS-BACKLOG). **Corrected**: only the **steering D term** (`steerKD`, live in the PD assembly) is still a feel-based carve-out — it was lowered because the previous value felt over-damped and is not grounded in measured tire behaviour. The **speed-based steering reduction** this note used to lump in with it (`steer /= 1 + factor * (fwdSpeed - threshold)`) is **not live**: it exists only inside the ZOMBIE block. **Everything is in Gs** (latG = `AverageAcceleration·right ÷ 9.8f`; `_bestLatG` stored in Gs; declared grip ceiling = `CurrentMechanicalGrip`, NOT × gravity — a lingering 9.8× unit bug lived here, never multiply grip by gravity and treat it as G). **The reference is now a linear proportional controller, not an exponential filter**: `ref += (Gs − ref) × gain × dt` (gain ≈ 5 → ~0.2 s time constant), symmetric rise/fall, no deadband — the input is spike-clamped before integrating (a collision impulse must not lift the reference even one frame's step). Utilization (current/ref) drives explore vs exploit: near the peak → exploit (limit = remembered steer + small margin, floored); below → explore (limit = `min(steerLock, bestSteer·1.5 + 2)`, floored — *bounded* per audit MAJOR-3, since full lock would map to ±1 input and make the clamp a no-op). Declared grip is only the spike-rejection sanity cap (`×1.1`). The two old limit systems (grip-saturation formula; slide-angle lerp) are commented out above. Hitting the limit caps `MaxThrottle` (pegged → 0.9, 10° past → 1/s cut). TRlat/declared curves are not trusted. (The `STEERING_AUDIT.md` file this section used to reference no longer exists in the repo; its surviving conclusions are the MAJOR notes above.)

## Pedal map (`ComputeCombinedInput`)

- One linear map for both pedals, **no feedforward**: `Clamp(speedError / divisor, ±1)`, the brake's divisor being the throttle's × `BrakeErrorMultiplier`. It replaced a `signsDisagree` shortcut that returned full brake for *any* deceleration above 10 mph — **redundant**, because a genuine direction swap's error is the sum of the magnitudes and saturates on its own.
- **What that shortcut was hiding is droop, and it is live again**: a P-only map holds a decel only at a standing error of `divisor × brakeFraction`, so under sustained braking the car tracks the plan *above* it by roughly that much. At the current divisor that is a handful of m/s; if braking ever under-brakes in long zones, this is the cause, not the decel formula.
- **The fix belongs on the pedal side, not the plan**: apply the plan's own demand as feedforward — `EffectiveBrakeFactor(apexNode)` is already the brake fraction it budgets for — and let the error trim. **Do not restore `PedalTrackingOffsetMps`/`BrakeDemandScale`** (added `b767c19`, removed `07d5e57`): that aimed the *solve* a fixed margin below the ballistic profile, which coupled the plan to the pedal map's gain and scaled the learner's output on its way into the solve.

## ShowCheckpoints — full marker details (restored verbatim from b845170)

Each corner renders **two primitives**:
    1. **Standing torus ring** — `(MarkerType)1` (the SHVDN2 enum hides this as `Cylinder` but the prop renders as the classic torus; cast through `World.DrawMarker` rather than raw `Function.Call` to dodge texture-arg marshalling). Anchored **below** `TrackPoints[corner.Node]` (dropped into the ground), X/Y scale = `trackPoints[node].TrackHalfWidth * 2` (full track width, so the ring spans the entire road at the apex), camera-facing. The wrapper does NOT crash like raw `DRAW_MARKER` with `textureDict=""` did — the wrapper sidesteps the marshalling issue.
    2. **Centered exit chevron** — `(MarkerType)20` (`ChevronUpx1` / `PROP_MK_RACE_CHEVRON_01`). Anchored **directly above** `TrackPoints[corner.Node]` (kept centered), lifted above ground (matching FiveM `client.lua:452`). Direction = `(nextApex - apex).Normalized` (zeroed Z); rotation per FiveM `client.lua:466–491`; `faceCamera=false` so the direction vector is honored.
  **LOD strategy**: a corner renders if and only if the node delta from the player is within a window with a small negative grace (so a ring doesn't pop the instant it's crossed; delta = node − playerNode, plain subtraction for point-to-point, modulo for circuits via `ARS.IsPointToPoint`). The player Racer is passed into `DrawCornerCheckpoints(Racer player, …)` — its live `CurrentTrackPoint.Node` (updated each tick by the normal track-position pipeline) gives the player node, no extra scan. Runs from `OnTick` (player found via `Racers.FirstOrDefault(r => r.Driver != null && r.Driver.IsPlayer)`), guarded by a `playerRacer != null` check. FiveM reference is `racingsystem/client/client.lua:393–491`; ARS adapts this to corners-only via `ARS.Corners` instead of every node.

## ShowEdgeChevrons — full marker spec (restored verbatim from b845170)

Small blue half-alpha chevrons (`(MarkerType)20`, scale 1, `faceCamera=false`, rotation `(89, 0, -90)`) on both track edges, pointing along the track direction, positioned exactly at the edge (`TrackPoint.Position ± cross(direction, up) × TrackHalfWidth`, +0.2 m lift). Drawn on **odd absolute node indices only** within ±30 of the player's node (modulo for circuits, clamped for point-to-point) — a fixed lattice on the track, since anchoring the stride to the player's node made chevrons slide strangely as the player advanced; player node from the passed-in player Racer's `CurrentTrackPoint.Node` — same pattern as ShowCheckpoints (both share the hoisted `playerRacer` lookup in `OnTick`). (`ShowTrackAnalysis` was the AI-debug view; it was retired with the other consumer-less toggles, `f1acebd`.)

## ShowPhysics (legacy, unreachable)

**ShowPhysics** — G-force sphere/vector. The toggle and its `DrawRacerDebug` gate are **gone** (`f1acebd`); the only surviving trace of that family is the never-assigned `DebugVisual` static, which two dead `DebugDisplay.PropEdit` reads still test.

## Vehicle-stat model-native hash table (restored verbatim from b845170)

Model-level natives take an **int model hash**, not a spawned handle. Confirmed-working set: `GET_VEHICLE_MODEL_MAX_TRACTION` (`0x539DE94D44FDFD0D`, grip in G), `_GET_VEHICLE_MODEL_ESTIMATED_TOP_SPEED` (`0xF417C2502FFFED43`, m/s), `GET_VEHICLE_MODEL_ACCELERATION` (`0x8C044C5C84505B6A`), `GET_VEHICLE_MODEL_MAX_BRAKING` (`0xDC53FD41B4ED944C`), and `GET_IS_VEHICLE_ELECTRIC` (`0x1FCB07FE230B6639`, returns 0/1 — **game build 3258+**, so `BuildPowerCache` probes it once and degrades to "no electrics" on a build without it, rather than throwing inside the per-model loop and losing that model's stats). The spawned-handle versions are *different* natives (e.g. `GET_VEHICLE_ACCELERATION` / `0x53AF99BAA671CA47`, `GET_VEHICLE_MAX_TRACTION` / `0xA132FB5370554DB0`) — don't mix them up. This is what makes pre-race vehicle evaluation cheap — no car needs to exist yet.

## Downforce derivation — source paths (restored verbatim from b845170)

See `E:\GTA\GTAVSP\GTAV Source\src\dev_ng\game\Vehicles\handlingMgr.h:875` (`m_fDownforceModifier` at `0x14`; line 874 is `m_fInitialDragCoeff` at `0x10` — that off-by-one is what produced the old wrong `0x0010` claim) and `script/commands_vehicle.cpp:6203` (`CommandGetVehicleMaxTraction` formula) for the source of truth. **The panthere verified end-to-end** (GRP `2.58`, DF `120` after offset fix + principled divider). Other cars with raw downforce `> 100` should be sampled for sanity before treating this as universally closed — the formula is correct, but it's only confirmed against one data point so far. (Context: the principled divider is `grip /= 1 + 0.035 × Handling.Downforce`; the previous empirical `grip /= 4 + Downforce/100` hack was a curve-fit to the wrong offset's value range.)

## Per-menu ini persistence — full details (restored verbatim from b845170)

`Menu-Race.ini` (Race submenu: Track, Laps, GridSize, PaceTarget — later extended with `PaceMode` + `PaceOffset` by the dual-pace work), `Menu-Settings.ini` (the Settings submenu: GridSorting, TimeoutSeconds, AIRacerAutofix, AiNitro, UseMenyooSkins), `Menu-Debug.ini` (all Debug-submenu toggle keys, named after the `Options` enum entries). (Those two files were `Menu-Racers.ini` and `Menu-DevSettings.ini` until the menu-scoped rename.) One section `[MENU]` per file, one key per persistable item (buttons never persist). `src\MenuSettings.cs` (`MenuSettings` class) is the store: one lazy `ScriptSettings` object per file (respects the single-ScriptSettings-per-file rule — never `Load` the same file twice), `Set` saves on every change, `Migrate(key, legacy)` is a one-time seed that writes a legacy value only if the new file lacks the key. **Live readers** (`PlaceCars` GridSorting, timeout in OnTick, `Racer.cs` AIRacerAutofix) read the stores directly (`ARS.SettingsMenuStore.GetInt(...)`). The old `ARS.RaceSettingsFile` static and `SaveOptionSetting` are gone; `Settings.ini`/`Options.ini` are **not** both legacy-only: `Settings.ini` is a one-time migration input, but **`Options.ini` is still read live** every race (`CATCHUP.OnlyLastHalf` / `OnlyBehindPlayer`, `GENERAL_SETTINGS.ReverseRoutes` in `TrackLoader`) and is never written. (These prose lists are superseded by the inventory tables below, which are exact.) Lists store **values, never indexes** (indexes break when option lists change); dynamic lists restore at population time (`RefreshTrackList` self-heals via the existing keep-chosen logic, `RefreshPowerControls` reads PaceTarget with an `IsNaN` guard — `Clamp(NaN)` returns the min bound, the known gotcha). Grid Size restore replaced the `_intendedOpponents = 4` hardcode in `LoadSettings`. The immersive join point (line ~1325, pace set to the player's car) deliberately does **not** save — session convenience, not menu state. The `arssettings` reload path re-runs `LoadSettings`, which recreates all three stores.

## Racers submenu — dead-code removal (restored verbatim from b845170)

The old dead-code `PlaceCars` body (local `GridSort` enum, `Team.Cop` reorder, inline position loop) was removed.

## Weaponized grid filter — full note (restored verbatim from b845170)

A 3-way "Weaponized Cars" menu option + `DOES_VEHICLE_HAVE_WEAPONS`-based LoadGrid filter were implemented then **removed/never shipped** because reliable per-model weapon detection is unavailable (no model-hash native exists; the spawned-handle native is spawn/state-dependent). Reimplement only once we can reliably know which car models carry weapons — the durable approach is a per-model marker in the roster file (`cars.txt`), not native probing.

## Pace selection — current state, and two lessons from the window→ranking switch

**Current**: selection is a **ranking** — `VehicleSelector.SelectClosestByPace` does one pool pass, sorts by `|pace − target|` and takes the closest N (shuffled for ties). The bracket, the widen loop (`Options.WidenBracketFill`), `PowerBracketScale` and `VehicleSelector.Select` (plus its never-called `CreateDuplicate`) are deleted; a stale `PaceBracket`/`WidenBracketFill` key in an existing ini is never read, because `LoadSettings` walks the `DebugToggles` **dictionary**, not the enum, so an orphaned key is simply not visited. `Pace Offset` is a fixed signed list (−10..+10, step 0.5, labels like "+2.5"); Relative resolves `playerPace + offset` **once per `InstanceGrid`** into a local, and must never be written back into `PowerTargetScale` — that write-back is the one real trap (it corrupts the persisted absolute target). Pace Mode alone decides the anchor for every grid; `ComputePlayerCarPaceIndex` (the join's old fallback wrapper) is gone with the join override. The b845170-era wording for all of the above (bracket + widen loop, offset −50..+50, `Clamp(playerPace + offset, 0..200)`) is in git at `b845170` if ever needed.

**Lesson 1 — clamping an anchor is inert for a ranking.** Clamping into `[min,max]` cannot change a distance *ordering*: every candidate's pace lies inside the span by construction (same cache), so both distances stay monotonic in pace and the sequence is identical. The fleet-span clamps were load-bearing only for a **fixed-width window** (moving a window's centre changes which cars fit), so they were removed with it. The single surviving span use is a diagnostic log when the anchor sits outside the fleet.

**Lesson 2 — the switch inverted non-finite-pace handling.** A window *excluded* non-finite paces (`NaN <= bracket` is false); `OrderBy` over a ranking sorts **NaN first** (`Comparer<double>` places NaN below every value), so a NaN score would have been preferred — the opposite of before. Root cause: `BuildPaceIndex` filtered grip and mph but **not** `accel`, the one raw stat taken straight from a native and defaulted via `TryGetValue`. The computed score is now validated before entering `ModelPaceIndexCache`. (An infinity would additionally have emptied the Pace Target list: `GetPaceSpan` has no filter, and `AddPowerValues` computes `(int)Math.Floor(Inf / step)`.)

**Electric detection — a real bug found and fixed (`96f5a72`)**: this file used to record `GET_IS_VEHICLE_ELECTRIC` as `0xD839450756ED5A80`. That hash **is not a native at all** — it isn't in SHVDN2's `Hash` enum and no member name contains "ELECTRIC" — and calling it returned non-zero for *every* model, so `ModelElectricCache` flagged all 488 cars as electric and the electric correction was silently applied **to the whole fleet** (petrol cars gained 5–9 PI). The real native is `0x1FCB07FE230B6639`, model-hash based, build 3258+; verified in game: 19 electrics, petrol reads 0. The lesson worth keeping: a native hash that nobody has verified in game is a guess, and a wrong hash here fails *silently and plausibly*. **Follow-up worth knowing**: `96f5a72` repointed only the *selection* path — `Racer.Initialize` and the player-pace fallback kept calling the discredited hash, so every car's **spawn-time** pace over-applied the electric bonus while the grid was ranked on the corrected flag. `Racer` now reads `ModelElectricCache` (the very flag selection used, which makes the two agree by construction) and the fallback calls `ARS.IsElectricModel`, which owns the single availability probe; the bad hash appears nowhere in `src`. "TopUpNitrous returns success" was an audit fragment that does **not** exist in any committed AGENTS.md — the nitro-pacing rework (`Racer.NitroChargedLap`, replacing the idle 1 Hz top-up) supersedes that line of inquiry entirely, so it is closed.

## Settings folder — declared inventory + repair (`3fa7737`, `0fd4e0a`)

`src\SettingsRepair.cs` is the inventory: which ini files ARS expects, which keys each must hold, and what a healthy value looks like. Three passes run from `LoadSettings`, in an order that is load-bearing:

1. `CreateMissingFiles()` — first, before anything reads a store. Creates `Settings\` and each **foreign** file from a literal template. Also prints the old "check you've installed ARS properly" case, now as a restoration notice.
2. `PruneOwnedFiles()` — still before any store reads its file, so no cached `ScriptSettings` object exists that could later `Save()` a pruned key back into the file. Line-level filter: inside `[MENU]`, a `key = value` line survives only if the schema declares the key; every other line (blank, comment, foreign section) is preserved verbatim. Rewrite goes through a temp file + `File.Replace` so no half-written ini can exist.
3. `CompleteOwnedKeys(...)` — last, **after** the legacy migrations and after the `LoadSettings` reads. Fills missing keys with their declared defaults and normalises rejected values, all through the existing `MenuSettings` API (`Get(key, null)` is a side-effect-free existence probe — `SeedIfMissing` returns early when the fallback is null).

**Why owned vs foreign** — `Menu-*.ini` are written by the menu stores, so the schema can own them outright. `Options.ini` / `DevConfig.ini` / `MemoryOffsets.ini` are hand-editable, and SHVDN's `ScriptSettings.Save()` writes back only the sections/keys it parsed: **comments are lost**, which would destroy the MemoryOffsets header. They are therefore created when missing but never rewritten (`DevConfig.ini` is additionally arrived at by **moving** `DevSettings.ini`, so its comments survive the rename). `Settings.ini` is a legacy migration *input* (its `[RACERS]` keys seed `Menu-Settings.ini`) — creating it would fabricate legacy state, so it is deliberately not expected.

**Invariant: a created file holds exactly the values the code would use if the key were absent**, so creating one cannot change behaviour. That is also why `Track` and `PaceTarget` are declared *optional* (no default): absence is meaningful for both (no track chosen; and an absent `PaceTarget` is what triggers the fleet-span middle autoselect in `RefreshPowerControls` — writing any default there would silently pick the slowest cars in the fleet instead).

**Value policy**: `Bool` and plain numbers must parse (garbage → declared default); a number with a domain list (`TimeoutSeconds`, `AIRacerAutofix`) **snaps to the nearest offered value** — an off-list value is what makes the menu silently show index 0 while the file holds something else (the long-standing wart in the Council backlog); list-backed strings (`GridSorting`, `AiNitro`, `PaceMode`) must match a spelling exactly, a case-only difference is rewritten to the canonical spelling, anything else resets to the default; `GridSize` is range-clamped to the same 0..12 the menu enforces. Optional keys (no default to repair them to) are handled by the prune pass instead: a declared **numeric** key that does not hold a finite number has its line dropped, because absence is its healthy state. That rule exists because of a defect caught in-game (`0fd4e0a`): `RefreshPowerControls` probed `PaceTarget` with `GetFloat("PaceTarget", float.NaN)`, and seed-on-read wrote the **sentinel** — a file containing `PaceTarget = NaN` — which additionally defeated the "absent → auto-pick the middle of the span" branch that key is declared optional for. **Gotcha: never hand a non-value sentinel (NaN) to a seeding getter as its fallback; probe with `MenuSettings.TryGetFloat` (non-seeding) when absence is meaningful.** A wrongly-typed *non-numeric* optional key (e.g. `Track = junk`) is still left alone.

**The inventory** (owned files, `[MENU]` section, in schema order):

| File | Keys (default) |
| --- | --- |
| `Menu-Race.ini` | `Track` *(optional)*, `Laps` (4), `GridSize` (4), `PaceMode` (`RelativeToMine` — the menu calls the item `Grid PI Mode` and spaces the option to "Relative To Mine"), `PaceOffset` (0), `PaceTarget` *(optional)*, `ReverseRoute` (False) |
| `Menu-Settings.ini` | `GridSorting` (Power), `TimeoutSeconds` (30), `AIRacerAutofix` (1), `SpeedOffset` (0), `SmartTuning` (True), `AiNitro` (IfPlayerHas), `UseMenyooSkins` (True), `OverspeedEnabled` (True), `BrakeLearning` (True), `RouteSpeedLimit` (True), `CrestEffect` (100), `HillGripEffect` (100), `StagedSpawns` (True) |
| `Menu-Debug.ini` | one key per `DebugToggles` entry — the table *is* the schema, so retiring a toggle retires its key automatically |

`Menu-Settings.ini` **carried the retired `PaceMode` key** (`9b8da25`) before the Settings store took that name: `LoadSettings` reads its `PaceMode` **before the prune pass** (the key is undeclared now, so the prune would drop it first) and carries it into `Menu-Race.ini`. That file is then pruned and repopulated as the Settings store, so this legacy read is one of the two that cannot move below the prune — the other being `Menu-DevSettings.ini`'s `BrakeLearning`/`StagedSpawns`. **The rename migration follows the same rule**: `Menu-Racers.ini` → `Menu-Settings.ini` and `Menu-DevSettings.ini` → `Menu-Debug.ini` are migrated key-by-key from `SettingsRepair.DeclaredKeys` (never a hardcoded copy of the key list) and the old files are then **deleted**, so the folder never holds the same settings under two names.

Foreign files created when missing: `Options.ini` (`GENERAL_SETTINGS` Laps/ReverseRoutes, `CATCHUP` OnlyLastHalf/OnlyBehindPlayer), `DevConfig.ini` (`GENERAL` Hotkeys/LoadAtStart/LogLevel, `CREATOR_DEFAULTS` TracksideModel/TracksideModelFrecuency), `MemoryOffsets.ini` (comment header + three `0x0` offsets). Neither a `Disciplines` key nor any per-car appearance data survives: the discipline selection filter was retired with the pace-bracket cleanup (`3fa7737`) and the `<Disciplines>` tags went with the whole vehicle-XML system (`d0a6e10`) — see the roster bullet in AGENTS.md.

Two wrinkles worth knowing: `ReverseRoute` lives in `Menu-Race.ini` (the Race menu's checkbox passes `RaceMenuStore` through the optional `store` overload), yet a `ReverseRoute` key **also** keeps appearing in `Menu-Debug.ini` because the load loop reads every `DebugToggles` key from the dev store before overriding that one from the race store — so the dev schema must keep it or the repair and the load loop would fight on every start. And `OverspeedEnabled` has no `Migrate` call at all; it is seeded purely by the `LoadSettings` read (seed-on-read), which is exactly the kind of key the completeness pass now guarantees.

**One resurrection path** (narrow, cosmetic): `InitializeMenu` captures store objects in the `store`-overload checkboxes, and an `arssettings` reload re-runs `LoadSettings` (new stores) *without* rebuilding the menu — so a stale handler can `Save()` a pre-prune in-memory view and briefly bring a pruned key back. The next load prunes it again. Same family as the already-known "`arssettings` reload leaves pace-mode enable-states stale until next refresh".

**What the repair does not do**: it does not validate that a `Track` path still exists on disk, does not reorder or reformat foreign files, and does not touch `Settings.ini`, `Tracks\`, `Vehicles\`, `Drivers\` or `sillynames.txt`.

## Vehicle livery/mod naming - moved to AGENTS-SMARTTUNING.md

The two-livery-spaces trap (mod slot 48 vs the livery list), the exact natives and the offline label tables now live in the smart-tuning companion, alongside that feature's other notes.

## Menu structure and items - moved out of AGENTS.md (size trim)

**LemonUI SHVDN2** replaces the hand-drawn menu (reference dll deployed beside `ARS.dll`); every menu is built in `InitializeMenu`. **Only a 10-row window is ever drawn** (`NativeMenu.maxItems`, LemonUI 1.5.2's default, never overridden): `Draw` iterates the cached `visibleItems` = `[firstItem, firstItem + 10)`, and `Add` appends — so an item added to a menu **at runtime lands below the fold** and reads as "it didn't work" until the selection scrolls it into view (the Race menu is 12 rows with the spawn items present, 10 without, so two rows sit off-screen). **Assigning `SelectedIndex` fires `ItemChanged`** — so a handler that touches *other* menu items must not run before those items exist: building Grid PI Mode above the two PI items it rewrites threw a constructor NRE that stopped the script loading entirely (`9b8da25`; the mode's index is therefore assigned after PI Target exists). Root (**ARS**) = the Race, Settings and Other submenus + **Reset To Track** as its last child — Reset is root-level despite the Race-flavoured comment beside it, and **Start Race is a Race item, not a root one**. Race: track selection, Laps, **Reverse Route** (`Menu-Race.ini`), **Spawn Track** (after Laps), grid size, the PI trio — **Grid PI Mode above PI Offset**, then PI Target — **Spawn Grid** (after the PI trio, immediately before Start Race), Start Race, **Restart Race** (same racers when `PlayerParticipating`, else a fresh grid via `StartRaceFromMenu`), End Race; **the two spawn items are six slots apart by design, not a pair, and both vanish while Staged Spawns is off**. Settings: General Settings + Advanced Settings + AI Settings + Debug submenus (it holds no items of its own). Other: Freecam. Opens via Sprint+Context (while `[GENERAL] Hotkeys`) or `arsmenu`, and only when nothing else is visible; LemonUI handles navigation/input suppression.

## General Settings / AI Settings / Advanced Settings - item inventory, moved out of AGENTS.md

**General Settings submenu** (under Settings; store file is `Menu-Settings.ini`): GridSorting (**live**: `PlaceCars` parses the `GridSort` enum and `GridBuilder.Place` sorts by mode, player always last, positions reversed for point-to-point), TimeoutSeconds, AIRacerAutofix, **Smart Tuning** (the livery/parts/paint pass - see `src\SmartTuner.cs`), **Racer Nitrous** (`AiNitro` is only the ini key, never a label; the `TriState` members are stored as `Never`/`IfPlayerHas`/`Always` and shown spaced — see the enum bullet in AGENTS.md — via `ARS.AiNitroAllowed()`; IfPlayerHas re-probes live, one shot per lap) and **UseMenyooSkins** (cosmetic gate on `MenyooAppearance.Apply`). **Sibling submenus** (both also `Menu-Settings.ini`): **AI Settings** = **Speed Offset (mph)** (the speed pipeline's one flat fudge), **Brake Learning** (the braking-decel sampler the AI reads live — it is a `static`, not a debug dictionary entry) and **Route Speed Limit** (On = route curvature limits sweeping corners; Off = the corner braking plan alone — also a live `static`), **Crest Effect (%)** and **Hill Effect (%)** (both live `static`s scaling a terrain grip-loss intensity — crest vertical curvature and hill pitch respectively; 0% ignores that terrain effect, 100% is the tuned default, and each scales *loss only* so a dip's grip bonus is never amplified); **Advanced Settings** = **Overspeed Correction** (`OverspeedEnabled`) and **Staged Spawns** — the only settings item with a side effect, adding/removing Spawn Track and Spawn Grid in the Race menu; an off→on cycle restores them **to their original slots** (it appended them past `End Race` until the slot indices were captured at construction). **Weaponized grid filter — DEFERRED**: reliable per-model weapon detection doesn't exist; rebuild only from a marker in the roster file (`cars.txt`), not native probing.

## Debug submenu - per-toggle detail, moved out of AGENTS.md

- **Show Inputs** — for the closest AI car (`DebugFocusRacer`): the white lane-aim line to the final target, plus two red wall stubs at the steer-reference node (baseline = the track edges, pulled inward as rivals wall the car off).
- **Show Projection** — the same `ProjectAhead` the pipeline reads, cut into three slices: 0.5 s (cyan — the lane-pursuit blend's read), 1 s (magenta — the off-track cap's read), 1.5 s (orange — read-ahead, **nothing consumes it yet**). Sphere plus a ground drop line at each cut; closest AI car only.
- **Show Input Trail** — the closest AI car's *applied* pedal input (`Throttle − Brake`, i.e. after the offshoot cap, TCS and the pedal slew) sampled at a fixed sub-metre spacing: spheres (green throttle → yellow neutral → red brake) plus a white stub from the car to the newest sample. Count-capped, oldest popped, cleared per race in `Initialize`; **keep the cap small — see the `DRAW_MARKER` budget gotcha** (spacing and cap are in the code: the sampling threshold only bites below ~30 m/s, above which one frame of travel already exceeds it).
- **ShowEdgeChevrons** (default off, as shipped) — half-alpha chevrons at both track edges, on **odd absolute node indices only** within a ±30-node window (a fixed lattice; a player-anchored stride slid strangely).
- **Retired (`f1acebd`)**: Show Card State, Show Track Analysis and Show Physics were menu entries with **no consumer at all** — they persisted to the ini and drew nothing. Removed from the menu and the `Options` enum (new toggles append, since values are positional); `SettingsRepair` prunes their stale keys. The `DebugDisplay`/`DebugVisual` (Inputs/Speed/Positioning/PropEdit) scaffolding is still dead — nothing ever sets `DebugVisual`.
- **`ReverseRoute` is the odd one out** — it sits in `DebugToggles` (so it is declared in `Menu-Debug.ini`) but its checkbox passes `RaceMenuStore`, and the load loop reads the dev key then overwrites it from the Race store on the next line: the dev key has **no effect**. It must stay declared, though — the loop's read is a *seeding* getter, so it re-creates the key every launch and the repair pass would otherwise fight it.

## Leaderboard - drawing and data detail, moved out of AGENTS.md

- Drawing lives in `DrawLeaderboard`, gated by the Show Leaderboard toggle. **Positions freeze per racer**: crossing the line stamps `FinalPosition` (finish order via `LeaderboardFinish`, mirrored into `RacePosition`, reset in `Initialize`); the live progress sort only touches cars with `FinalPosition == 0`. On race end (all finished or timed) stragglers are classified by progress and appended.
- **Draw**: finishers in locked order first, then still-racing cars numbered `finishedCount + rank` by live progress; the player's row is yellow. The finish/reward block awards `RaceReward` to the first finisher, sets `RaceStatus = Finished` and calls `CleanEverything()` (HUD/leaderboard only draw during Countdown/InProgress).
- **Data**: `RacePosition`/`FinalPosition`/`Name`/`Lap`, `Pressure`, `_raceTimedFinishMs`, `RaceReward`, `ParseToTimeSpan`. Two write-only fields sit alongside them: `LapTimes` (the player laptime notification uses the local `lapTime`, not the list) and `VehicleData.TextPerformanceIndex` (the pace prefix is baked into `Name` from `PowerScale`) — neither has a reader; wire them up or prune them.

## Grid pace selection - pace score, electrics and PI ballpark, moved out of AGENTS.md

- **Pace score** (`ModelPaceIndexCache` + `ComputePaceIndex`): a raw per-model score, **not** 0–1 (`effectiveAccel × k + topSpeedMph + grip × k2`; the old normalized/ceiling/weights consts were dead code, since removed). **Electrics** are flagged live at table-build time by `GET_IS_VEHICLE_ELECTRIC` (model-hash native, build 3258+, probed once so an older build degrades to "no electrics") and their raw G is scored at the **midpoint of the game's own drive-force ramp** — ×5 at standstill falling linearly to ×0.9 at top speed, so ×2.95 — with **no top-speed discount** (the 0.9 is the ramp's endpoint, not a reduction of top speed; the old code misread it that way *and* used a hash that flagged all 488 cars as electric). The `arsepidump` cheat dumps the whole table to `pidump.txt` for this kind of analysis. `Racer` recomputes at spawn with the same function, reading the **cached** flag selection used (`ModelElectricCache`, falling back to `ARS.IsElectricModel` for a car the roster never scored), so **the leaderboard number equals the selection metric**.
- **PI ballpark in the menus**: the PI Target and PI Offset descriptions append three example cars — nearest at or below the value, nearest overall, nearest at or above — read from `ModelPaceIndexCache` and named via `ModelNameCache`. That cache resolves the **GXT label** the model native returns (`GET_DISPLAY_NAME_FROM_VEHICLE_MODEL` → "SABREGT") through `Game.GetGXTEntry` to get the readable name, falling back to the roster line for an add-on the game can't resolve. The offset's trio is `playerPace + offset` and is omitted while the player is on foot; `ApplyPaceModeUI` is the single writer of both descriptions (it also carries a null guard — the item handlers can fire mid-build).